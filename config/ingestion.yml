dataset:
  dataset_id: demo_12w_2owners_example
  window:
    mode: absolute    # relative|absolute
    weeks: 12
    end_date: null
    start_date: 2025-10-01
    end_date: 2025-12-31

credentials:
  file: secrets/credentials.json

gmail:
  enabled: true
  owner_mailboxes:
    - mbehotas@celaralabs.com
    - ctommasi@celaralabs.com
  query:
    extra: ""         # optional extra Gmail search terms
  max_per_mailbox: null
  page_size: 200

slack:
  enabled: true
  include_channels: null   # null => all channels bot is member of
  exclude_channels:
    - random
    - social
  include_archived: false
  page_size: 200
  max_per_channel: null

filters:
  min_text_len: 20
  drop_if_sender_contains:
    - no-reply
    - noreply
